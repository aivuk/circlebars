// Generated by CoffeeScript 1.6.3
(function() {
  var chart, chartHeight, circles, metas, numbers;

  numbers = [[1, 2], [2], [4, 5, 6]];

  chartHeight = 650;

  metas = {};

  d3.csv("/data/metas.csv", function(d) {
    var ms, o, _results;
    d.forEach(function(r) {
      if (metas[r['objetivo']] != null) {
        return metas[r['objetivo']].push(r);
      } else {
        return metas[r['objetivo']] = [r];
      }
    });
    _results = [];
    for (o in metas) {
      ms = metas[o];
      _results.push((function(ms) {
        return ms.forEach(function(m, i) {
          return chart.append("svg:circle").attr("class", "circ").attr("cy", chartHeight - (i * (40 + 5)) - 40).attr("cx", parseInt(o) * (40 + 5) + 40).attr("r", 20).attr("fill", "blue").on("mouseover", function() {
            return d3.select(this).transition().attr("fill", "red");
          }).on("mouseout", function() {
            return d3.select(this).transition().attr("fill", "blue");
          });
        });
      })(ms));
    }
    return _results;
  });

  chart = d3.select("#circlebars").append("svg:svg").attr("width", "100%").attr("height", "100%");

  circles = d3.selectAll(".circ");

}).call(this);
